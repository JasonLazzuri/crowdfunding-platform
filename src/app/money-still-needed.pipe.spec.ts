import { MoneyStillNeededPipe } from './money-still-needed.pipe';

describe('MoneyStillNeededPipe', () => {
  it('create an instance', () => {
    const pipe = new MoneyStillNeededPipe();
    expect(pipe).toBeTruthy();
  });
});
